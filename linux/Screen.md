## command
```bash
$screen         #start screen

$screen -dr     #

ctrl + ac       # create a new terminal
ctrl + aa       # change terminal to between current one and previous one
ctrl + ap       # change terminal to previous one
ctrl + an       # change terminal to next one
ctrl + d        # exit current terminal
```